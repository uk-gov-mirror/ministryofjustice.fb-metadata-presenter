<%=
  f.govuk_collection_check_boxes component.id.to_sym,
    component.items,
    :id,
    :name,
    :description,
    legend: { text: input_title },
    hint: {
      data: {
        "fb-default-label-text" => default_text('label'),
        "fb-default-hint-text" => default_text('hint')

      },
    },
    include_hidden: false
%>
